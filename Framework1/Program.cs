using Framework1.Contexts;
using Microsoft.EntityFrameworkCore;

namespace Framework1
{
    internal class Program
    {
        static void Main()
        {

            //AppDbContext context = new AppDbContext();

            //context.Employees.Where(E => E.EmpId == 0);

            //context.Database.Migrate();

        }
    }
}
